const Spacer = ({ height = "1rem", width = "100%" }) => {
  return (
    <div
      style={{
        display: "block",
        height: height,
        width: width,
      }}
    />
  );
};

export default Spacer;
